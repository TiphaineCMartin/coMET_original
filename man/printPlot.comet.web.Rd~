\name{print.plot.comet.web}
\alias{print.plot.comet.web}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
%%  ~~function to do ... ~~
}
\description{
%%  ~~ A concise (1-5 lines) description of what the function does. ~~
}
\usage{
print.plot.comet.web(config.var, gbl.var)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{config.var}{
%%     ~~Describe \code{config.var} here~~
}
  \item{gbl.var}{
%%     ~~Describe \code{gbl.var} here~~
}
}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
%%  ~Describe the value returned
%%  If it is a LIST, use
%%  \item{comp1 }{Description of 'comp1'}
%%  \item{comp2 }{Description of 'comp2'}
%% ...
}
\references{
%% ~put references to the literature/web site here ~
}
\author{
%%  ~~who you are~~
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{
##---- Should be DIRECTLY executable !! ----
##-- ==>  Define data, use random,
##--	or do  help(data=index)  for the standard data sets.

## The function is currently defined as
function (config.var, gbl.var) 
{
    imagetype = config.var$IMAGE.TYPE
    imagename = config.var$IMAGE.NAME
    imagesize = config.var$IMAGE.SIZE
    imagetitle = config.var$IMAGE.TITLE
    if (imagetype == "pdf") {
        tmp.image.name <- paste(imagename, ".pdf", sep = "")
        pdf(encoding = "ISOLatin1.enc", file = tmp.image.name, 
            onefile = FALSE, width = as.numeric(imagesize), height = as.numeric(imagesize), 
            paper = "special")
    }
    else if (imagetype == "eps") {
        tmp.image.name <- paste(imagename, ".eps", sep = "")
        postscript(encoding = "ISOLatin1.enc", file = tmp.image.name, 
            horizontal = FALSE, onefile = FALSE, width = as.numeric(imagesize), 
            height = as.numeric(imagesize), paper = "special", 
            pagecentre = TRUE, fonts = c("sans"))
    }
    else {
        stop("Invalid image type: ", imagetype, " and image size: ", 
            imagesize, " combination.\n")
    }
    gbl.var <- draw.plot.comet.web(config.var, gbl.var, newpage = TRUE)
    while (dev.cur()[[1]] != 1) {
        dev.off()
    }
  }
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ ~kwd1 }
\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
